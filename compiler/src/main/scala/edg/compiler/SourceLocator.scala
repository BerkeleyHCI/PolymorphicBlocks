package edg.compiler


object SourceLocator {
  val empty = new SourceLocator()
}

// Source locator class, wrapper around the proto. TODO: should this just be the proto when it exists?
// TODO currently just a placeholder
class SourceLocator {

}
